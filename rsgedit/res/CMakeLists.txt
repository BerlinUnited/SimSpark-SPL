########### install files ###############
# Should we install these files?!
#install(DIRECTORY ./ DESTINATION ${DATADIR}/${CMAKE_PROJECT_NAME}
#        FILES_MATCHING PATTERN "*.xpm" PATTERN ".svn" EXCLUDE)
